# Tagging listings with an LLM. Get a key at https://openrouter.ai/keys
# Without it, tagging falls back to keyword matching and the build still works.
OPENROUTER_API_KEY=

# Optional: any OpenRouter model id. Defaults to deepseek/deepseek-v4-flash.
# OPENROUTER_MODEL=deepseek/deepseek-v4-flash
